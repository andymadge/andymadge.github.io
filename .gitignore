_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor
.bundle
Gemfile.lock
.claude/settings.local.json
assets/local-tests/
.tmp
audio_files

# Dev-only mix files (local development, not for production)
_djmixes/dev-*
assets/djmixes/dev-*/

# Serena MCP tool config (local dev only)
.serena/
